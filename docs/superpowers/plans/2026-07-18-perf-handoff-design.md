# Binding design — performance handoff implementation (2026-07-18)

Source spec: `../../../../docs/performance-reports/performance-review-2026-07-17-handoff.md` (owner-approved 2026-07-17).
Scope this run (owner, 2026-07-18): items 1–10 + 12. Item 11 deferred to its own re-freeze window; item 13 recorded for the release checklist only. Sweep policy = AGENTS.md note only. Item 7 proceeds automatically if its profile gate is met.

Every decision below is **BINDING** on planners and implementers: plan/implement, don't redesign. Research corrections to the handoff (verified 2026-07-18) are folded in.

## Shared constraints (travel to every task)

- Commit directly on `feature/ui`; no branches/worktrees; no commit trailers; never name the owner.
- Byte-identical gate for items 1, 6, 7, 8, 12.1, 12.2, 10-swap: `diff -rq <baseline> <new> | grep -v export_stamp` empty; UI PNGs `cmp`-identical; `cargo test -p starbreaker-ui` freeze SHAs green untouched. Item 2 has a **case-modulo oracle** (below). Item 9 has a **pixel-identity oracle** (bytes change by design).
- Builds sequential (shared `target/`); debug `cargo build` for iteration, release only for benchmarks/deploy. No concurrent cargo anywhere.
- Benchmarks: blanket-approved, but check `uptime` load < 2 first; benchmark window closes ~07:00 today.
- Pre-change oracles already captured: `ships_perf_bench/clipper_pre1` (+`clipper_pre2` determinism twin, logs `clipper_pre{1..3}.log`: 48.0–49.0 s, ~296 % CPU, 9.1 G RSS) and `ships_perf_bench/carrack_pre1`/`pre2` (in flight). Canonical export command per handoff; P4K auto-detected.
- TDD where a test surface exists; every new `.rs` file gets a `//!` header; no hard-coded game-data values; no `.unwrap_or` silencers; root-cause fixes only.
- After each landed item: append Observed/Finding/Action entry to `StarBreaker/docs/optimisation-ledger.md`. One commit per coherent item.
- Never-simplify list: the byte/pixel oracles, the determinism double-run checks, the item-8 pre-check gate, input handling at trust boundaries, the addon test suite.

## Decisions

**D1 (item 1).** Hoist `flate2 = { version = "1", features = ["zlib-rs"] }` into the existing `[workspace.dependencies]` (root Cargo.toml:35, precedent: parking_lot); the four crates (p4k:11, ui:8, gfx:8, blend:7) switch to `flate2 = { workspace = true }`. No other crates use flate2. Verify vs clipper_pre1 oracle + timing before/after.

**D2 (item 2).** Root cause = three divergent casing authorities: `texture_relative_path` (P4K entry case), `mesh_asset_relative_path` (source-string case; `let _ = p4k;` at decomposed.rs:3879 is the seam), `material_sidecar_relative_path` → `normalize_material_source_for_manifest` (force-lowercase after `Data/`). Fix: unify **all three** on `normalize_source_path` (P4K-entry casing, deterministic, run-independent). `OutputFiles.case_index` stays as belt-and-braces. TDD: unit tests asserting both fixed builders return P4K casing regardless of input-string casing (in-memory P4K fixture if a testable `MappedP4k` constructor exists, else integration-gated — planner decides after checking `owned.rs`).
  - **Oracle for this item only:** post-fix export vs pre-change baseline must be identical **modulo path casing** (compare with case-folded path sets + per-file `cmp` of content); any non-case difference is a bug. Two-run determinism diff must be strictly empty. UI PNG bytes unchanged.
  - Lands AFTER all byte-identical exporter items (1, 6/7, 8, 12.1/12.2) so their oracles stay valid; after landing, capture fresh Clipper/Carrack baselines for anything later.
  - Cleanup (2d): verify `ships/Data/objects` strict byte-subset of `ships/Data/Objects` per shared subdir, delete lowercase tree; `materials` (26 M) is NOT a subset of `Materials` (14 M) — merge unique files into canonical casing, then delete. Scene.json references must match on-disk casing after the fix (research gap b2).

**D3 (item 6).** Step 0 first: post-zlib-rs re-profile per June doc Phase 0 (`SB_UI_TIMING=1 RAYON_NUM_THREADS=1 RUST_LOG=info` Clipper export; capture MFD/HUD/medical per-stage rows; also one parallel run for production wall). Append dated block to `docs/StarBreaker/starbreaker-ui-perf-baseline.md` (existing format). Fast path: **two independent LUT substitutions** in `blend_premul_linear` and `blend_premul_add_linear` (colour.rs:72/:100): when `src[3]==255` use `u8_to_linear(src[c])` for the source decode; when `dst[3]==255` likewise for the dst decode. Bit-exact per `lut_matches_powf_helper_for_all_bytes` (colour.rs:131). **No opaque copy short-circuit** (encode∘decode roundtrip is not provably identity — LUT substitution only). Encode stays `powf`. Test: exhaustive `assert_eq!` fast==slow over all 256 channel values on each alpha-255 boundary, both functions.

**D4 (item 7, conditional).** Gate: item-6 profile still shows swf_load/stage-parse top-3. If met: parse-once per Appendix B3a — `parse_tags(bytes)` helper; extractors become `*_from_tags(&[Tag])` with `extract_*(bytes)` thin wrappers (existing call sites/tests compile unchanged); `SwfAssetLibrary::new` parses once, runs all 8 extractors + captures stage size/frame/visual-bounds into owned fields (kills per-call re-parse at library.rs:154-174); `merge_swf_bytes` one parse. Drop `raw` if nothing else needs it. **Preserve tag iteration order exactly**; keep two-pass structures (`DefineFontInfo` pass 2, font-edit-text metrics, ShowFrame counter); do not regress the CharacterId-sorted determinism fix. Note there are THREE per-image build sites (pipeline/mod.rs:540, :595, :750) — parse-once helps all three; consolidating builds (B2e) only if it falls out naturally, not a redesign. One commit, bisectable. Test: parse-count probe (`#[cfg(test)]` AtomicU64 in `parse_tags`) asserting one parse per construction + full regression suite.

**D5 (item 8).** New cache `HashMap<String, Result<RoughnessTextureLoad, RoughnessTextureLoadError>>` keyed by RAW source path (matching ddna_status_cache key semantics). Built by a new prewarm fn mirroring `prewarm_decomposed_textures`, enumerating via `normal_gloss_ddna_source_paths` over the same asset set as `prewarm_assets` (blend_assembly.rs:939-977), `par_iter` → `load_roughness_texture_result` (pipeline/textures.rs:1209 — note `pipeline/` subdir). Threaded into `write_decomposed_export` exactly like `png_cache` (blend_assembly.rs:978-1036 → decomposed.rs:1024/:1114); non-blend callers pass an empty cache. `export_ddna_roughness_asset_with_status` consults it before `load_roughness_texture_result` at :4950 only — **never touch `ddna_status_cache`** (early-return at :4941 precedes the `files` insert; prewarming it drops every roughness PNG). **Pre-check gate:** temporary probe timing serial vs `par_iter` over ~50 sources on Carrack; proceed only on >4× effective speedup, else record dead-end in ledger and stop. Verify: Carrack+Clipper case-aware oracles, two-run determinism, max-RSS not materially above 9.1 G/baseline Carrack RSS, timing deltas to ledger. Unit test: prewarmed source ⇒ identical `(TextureExportRef, TextureDerivationStatus)` AND roughness PNG present in `files`.

**D6 (item 12.1 + 12.2, batched).** 12.1: precompute `Vec<(normalized_cgf, normalized_material, cache_key)>` indexed by `mesh_index` over `unique_cgfs` before the container loop (~decomposed.rs:1555); replace recomputations at :1585-1590 and :1756-1760. 12.2: `prewarm_decomposed_textures` returns its `mtl_cache` (HashMap<String, Option<MtlFile>>, :4814) alongside the PngCache; thread into `write_decomposed_export` to seed :1111, mirroring png_cache. Both byte-identical; one commit.

**D7 (item 12.3).** **REJECTED — guard failed.** The layout-cache hit path skips `_restore_generated_decal_host_variant_polygons` and `_rebind_mesh_decal_for_host` (per-mesh, geometry-dependent, not keyed); with `data_pointer` dropped, two distinct meshes could share an entry and the second mesh's per-mesh fixes never run. Record as ledger entry (Observed/Finding/Action: rejected with evidence, what would discharge the guard). No code change, no test.

**D8 (item 3).** **One-loop lazy-index** in `starbreaker_addon/manifest.py::resolve_path` (:854 — NOT runtime/importer/): `path_index = None`; per candidate do direct `.exists()`, then lazily build index on first miss. Preserves exact per-candidate direct-then-index precedence (the handoff's literal two-loop version changes precedence when candidates==2 — rejected). TDD: `test_direct_hit_does_not_build_path_index` (tmpdir + minimal PackageBundle; assert result and `_path_index is None`); verify it fails pre-fix.

**D9 (item 4).** `builders.py:326` `tmp.pixels[:]`→`tmp.pixels[0:4]`, `:333` likewise. `orchestration.py:1247` untouched. No POM fixture exists; add one minimal test for the :333 fallback branch (stub `bpy` per test_pom_detail.py preamble; object with `.pixels` list, no `.filepath`; assert luma formula) — fails pre-stub? (it's behaviour-preserving, so the test asserts the value, written alongside the change).

**D10 (item 5, RE-SCOPED — handoff premise stale).** Disk is now 82 % used / 192 G free; `target/debug` does not exist; the 46–66 G reclaim target is void. Additionally `ships/dcb_canvas` IS read by `crates/starbreaker-ui/tests/live_ir_harness/mod.rs:253` (gracefully skips if absent — deleting silently disables the live-IR regression guard), contradicting the handoff's "nothing reads it". Therefore: **do NOT delete dcb_canvas** (surface to owner in the end-of-run report with the evidence); prune `graphify-out` to live graph.json + 3 newest snapshots (~165 M); leave the release-deps bisect ladder alone (it is 1.2 G in `release/deps`, not the handoff's mislocated 40 G debug claim); add a short retention-policy note to AGENTS.md §Building (`cargo sweep --time 30` for any regrown debug tree, keep release-deps ladder, graphify keep-3). No cargo-sweep install needed now (nothing to sweep).

**D11 (item 9 spike, RE-SCOPED).** `image` 0.25.10 already links png 0.18.1 — fast mode reachable today via `PngEncoder::new_with_quality(w, CompressionType::Fast, FilterType::Adaptive)`; no version bump. Dual-png collapse belongs to item 11 (tiny-skia 0.11/Tauri), out of scope. **UI PNGs are excluded** (live freeze gate hashes PNG bytes → would force a re-freeze; that batches with item 11). Spike scope: texture-PNG writes only — `starbreaker-3d/src/pipeline/textures.rs::encode_png`/`encode_png_rgba` (+ `cli/src/dds.rs:290`, gfx encode helpers if trivially shared). Measure: texture-export encode wall-time + total output-size delta on Clipper (and Carrack if time); pixel-identity via decode-and-compare of every changed PNG. **Spike does not land**: capture numbers + a patch file in the report/ledger; owner decides the size-vs-speed tradeoff. Working tree reverted after measurement.

**D12 (item 10 spike).** Phase 1: timing probe in `starbreaker-p4k/src/archive.rs::zstd_decompress` (:796) — `Instant` + `AtomicU64` nanos accumulator, one `log::info!("[timing][p4k] zstd_decode_total …")` at export end (match existing `[timing]` idiom). Measure share of Carrack export. If ≤5 %: record KEEP in ledger, stop. If >5 %: swap the p4k decode to `zstd::stream::decode_all`/`Decoder` (zstd 0.13 already linked via -blend/mcp) — byte-identical by format. chf: swap its DECODE (:62) too; its ENCODE (:82, `ruzstd::encoding`) may only be dropped if planner verifies chf encode is not part of any export/regression output whose bytes are gated (different zstd implementations emit different frames); if it is gated or unclear, keep ruzstd for chf encode and record the partial-consolidation tradeoff in the ledger for the owner.

**D13 (item 12.4).** Option (b): `#[tokio::main(flavor = "current_thread")]` in mcp/src/main.rs:16 + `tokio = { version = "1", features = ["rt", "macros", "io-std"] }` (the handoff's feature set alone won't compile — `#[tokio::main]` defaults to multi-thread requiring `rt-multi-thread`). If rmcp needs more (e.g. io-util), add the minimal missing feature rather than reverting to full. Redeploy per AGENTS.md §MCP (pkill -x, build --release -p starbreaker-mcp, cp). Verify server handshakes (MCP tool call from session or a stdio smoke).

**D14 (item 13).** No action. One paragraph appended to the end-of-run report + a release-checklist note (where the repo keeps one; else ledger entry) restating the scrub procedure and its pre-conditions.

## Implementation order (wall-clock aware; benchmark window closes ~07:00)

1. **Phase A** — item 1 (zlib-rs) + oracle + timing. Then immediately **item 6 step 0 profile** (needs post-zlib numbers) and **item 10 probe** + **item 8 pre-check** measurements while the window is open. These four benchmark tasks are the deadline-critical path and run serially (shared target/, quiet machine).
2. **Phase B** — item 6 fast path (+ item 7 if gated in), byte-identical.
3. **Phase C** — item 8 implementation (if pre-check >4×), item 12.1+12.2. Byte-identical.
4. **Phase D** — item 2 fix + TDD + cleanup + fresh baselines (case-modulo oracle).
5. **Phase E** — spikes: item 9 measurement (report-only), item 10 swap if >5 %.
6. **Phase F** — addon items 3+4; ops items 5 (re-scoped), 12.4; ledger entries incl. 12.3 rejection + 13 note; docs sweep.
Addon/ops tasks (F) are independent of Rust phases and may interleave whenever no cargo build is running; addon tests don't touch target/.
Each phase ends with its review task (code-review with security+performance lenses + spec compliance vs this design).
