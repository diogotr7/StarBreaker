# UI Fallback Register

> Satellite doc — the authoritative process is `crates/starbreaker-ui/docs/ui-workflow.md`; commands/tools/data live in `crates/starbreaker-ui/docs/ui-reference.md`.

This register tracks active UI fallbacks in `starbreaker-ui` and explicitly records owner, scope, trigger signal, and retirement target.

**Discriminator (mirrors `AGENTS.md` §Coding Practices).** The difference between a LICENSED host-side fallback and a banned hard-coded copy is EVIDENCE LEVEL, not kind: a numeric constant is a licensed host constant only when it is (a) proven absent from DataCore/P4K/AVM1 by an exhausted, blocker-grade search AND (b) registered here with owner, trigger signal, and sunset target. Anything plausibly data-sourced — or unregistered — is a violation to fix or flag. A blessed host constant (the MFD 44px inset, the annunciator 25px frame — both in the table below) and a flagged hard-coded table (the heading font-size table, §Flagged hard-coding) are the same rule at two evidence levels, not two rules. Licensed constants live in **Active Fallbacks**; flagged violations awaiting a fix live in **Flagged hard-coding** — never blend the two.

## Active Fallbacks

| Component | Fallback | Owner | Scope | Trigger Signal | Retirement Target |
| --- | --- | --- | --- | --- | --- |
| `crates/starbreaker-ui/src/bb_atlas.rs` | Manufacturer/gen atlas fallback paths | UI pipeline | Asset resolution when exact texture path is missing | `fallback_counter` in pipeline diagnostics and unresolved asset refs | Replace with authoritative source path resolution for all certified families in Phase 6B |
| `crates/starbreaker-ui/src/bb_loc.rs` | Ordinal localization parameter fallback | UI data resolution | Missing explicit named localization parameters | Localization fallback counters + unresolved localization warnings | Retire when localization map coverage proves complete on certification sets |
| `crates/starbreaker-ui/src/defaults.rs` | Versioned static default-value registry | UI data policy | Placeholder values for unresolved bindings/localization | `fallback_counter` diagnostics + unresolved binding warnings | Replace with authoritative runtime/default source extraction after fallback trigger rate is near-zero |
| `crates/starbreaker-ui/src/style/loader.rs` (`neutral_fallback`) | NEUTRAL white-on-black style when no `BuildingBlocks_Style` record (or palette slot) resolves — deliberately not a brand palette so missing data is visibly unstyled | UI style selection | Missing/invalid style source resolution | Selected style provenance + fallback warnings ("using neutral fallback") | Retire once style selection is always sourced from canonical style data for certified families |
| `crates/starbreaker-ui/src/bb_layout.rs` | Unknown sizing behavior defaults to fill-parent | Layout resolver | Unknown `sizingBehavior` values | Layout warning telemetry and unusual rect diffs in certification output | Replace with explicit sizing behavior coverage + hard-fail in non-release checks |
| `crates/starbreaker-ui/src/text/swf_draw.rs` (`swf_space_advance_px`) | Word-space advance for SWF text = the font's own space-glyph advance (data-backed); `0.33×em` only as a fallback when the font has no space glyph | Text renderer | Every space character | Width drift on multi-word text vs the reference | Re-derived 2026-06-08 against the in-game reference: the Clipper medical header space matches the font's space-glyph advance (~0.22×em); the previously-tuned `0.33×em` rendered the header space ~50% too wide (22px vs 15px). Now uses the font advance directly (the `0.33` fallback is retired to a no-space-glyph guard). |
| `crates/starbreaker-ui/src/mfd_view.rs` (`HOST_CONTENT_INSET = 44.0`, `HOST_STAGE_SIZE = (1280, 720)`) | GFx host content-view placement: 44px in from left/right/bottom of the host stage, flush top | MFD frame composition | Every framed MFD content view | Content-view position drift vs MFD captures | Measured framework constant: the placement is PROVEN absent from records, static SWF placement, AND the full AVM1 bytecode (plan P2.2a, 2026-06-12 — `examples/swf_avm1_dump.rs` over all 127 classes finds no 44/1192/676) — it is C++-host-side. `HOST_STAGE_SIZE` could additionally be read from the host SWF header (`SwfAssetLibrary::stage_size`, as `host_stage_text_scale` already does) — do that when next touching mfd_view. Retire the inset only if a data source for the view placement is ever found. |
| `crates/starbreaker-ui/src/ir_compose/engine_01.rs` (right-anchored caption-pair top padding `derived×1.5 + 0.25×label_em`) | Reproduces the reference-verified placement of the RIGHT-ANCHORED LabelCaptionPair (eob MEDGELS) whose COMPONENT RECT is misplaced by the med2 multi-canvas slot composition (rect y=-15.2; the meter anchored to its bottom lands ~35px above the capture's) | UI compose | Right-anchored (anchor.x≥0.99) caption pairs only | eob MEDGELS label/value/meter cap-tops vs `mesh_end_screen_plane-[medical2].png` (capture 34/63/84; render 37/66/84) | Added 2026-06-13 (plan P3.4): the retired TTF-inflated heights entered this arm twice (×1.5 padding + 0.25-em band centring); the pin preserves that output explicitly. RETIRE with the med2 slot-composition rect fix (the parked med2 arc). |
| `crates/starbreaker-ui/src/ui_ir/engine_01.rs` (annunciator 25px edge frame) | `root_annunciator_items` padding raised to a uniform 25px final-pixel frame (top/bottom pre-divided by the vertical canvas scale) | UI IR compile | The annunciator physical-screen render | Annunciator strip inset drift vs `Screen_Annunciator_L.png` (gold pins) | Measured engine pin, tuned 2026-05-31 (commit 11562b3e7). Derivation exhausted (plan P5.2, 2026-06-12): authored canvas padding is 15 TRBL with NO drak override; mrai/rsi author `Root_Annunciator_Items` Padding* brand entries (none 25); `M_Physical_Screen`, staticVariables, and the full AVM1 dump are all empty of 25 — C++-host-side, same family as the MFD 44px inset. Retire if an engine-side data source surfaces; apply the brand Padding* entries via the cascade when mrai/rsi ships onboard. |
| `crates/starbreaker-ui/src/hybrid_compose.rs` (`is_full_stage_swf_host`) | Full-stage SWF overlay fires ONLY on a Flash node whose entire subtree paints no BB content (pure placeholder); ordinary `rendererType:"Flash"` primitives with real BB children keep their BB subtree | UI hybrid compose | Every `is_flash_renderer` node considered for the overlay | Overlay is empirically DORMANT — 2026-07-07 IR dumps: DRAK Clipper 27/27 canvases `renderer_hint:bb` (annunciators resolve a SWF but stay `bb`, no `WidgetCustomShape`), ANVL Carrack 14/14 `bb`; 0 screens take `Swf`/`Hybrid` | Defensive guard (B3); the three named B3 hybrid gaps (Furore font, MFD footer, 4:3-vs-16:9) are already carried by the BB layer (see `.superpowers/sdd/B3-research.md`). `paints_bb_content` is a structural category rule covering all paint-bearing fields PLUS the one node_type-driven paint (`WidgetManufacturerLogo`); over-inclusion is the safe direction. Retire the overlay entirely only if BB is confirmed to carry every full-stage case. |
## Flagged hard-coding — unlicensed, pending derivation

These are NOT licensed fallbacks — they are game-DATA values copied into source (AGENTS.md ban) that are **not** proven absent from data. They are registered here as tracked debt (the "flag" arm of "fix or flag"), not blessed. Do not extend them; retire by deriving the value.

| Component | Hard-coded value | Owner | Why unlicensed | Trigger / tracking |
| --- | --- | --- | --- | --- |
| `crates/starbreaker-ui/src/compose/text_draw.rs:98-110` (`font_size_from_style`) | Fixed heading/body font-size table (`Heading1=>48, Heading2=>36, Heading3=>28, … Body=>16, Caption=>12`); SUB DECK renders `Heading3=>28` | UI text rendering / SUB DECK font arc (owner-deferred, ledger 107) | Brand typography `FontSize` entries copied into source, not proven absent from data. Likely the velocity/master-mode "authored-but-unapplied" pattern — the real brand `Heading3` fails to apply and falls back to 28 — so it is a data-sourced value, not a host constant. No exhausted-search evidence exists for it. | See `crates/starbreaker-ui/docs/ui-residual-budget.md` **R2** (owns the measurement + fix trail; do not duplicate). Fix = data-backed heading sizes (compass height-driven-labels pattern); code fix is OUT of alignment-plan T12 scope (belongs to the SUB DECK arc, ledger 107). Note: ledger 107 derived the render cap-ratio as `0.636 (= 28/44)`, but the live `Heading1` constant is 48, so the table's own ratio is `28/48 = 0.583` — reconcile this constant-vs-ledger divergence in the SUB DECK arc. |

## Recently Retired / Reduced Fallbacks

| Component | Previous Fallback | Status | Evidence |
| --- | --- | --- | --- |
| `ir_compose` + `bb_layout` | `TEXT_RENDER_SIZE_CALIBRATION = 1.5` + `LAYOUT_TEXT_MEASURE_CALIBRATION = 1.5` (TTF fallback draw/measure inflation) | Retired (2026-06-13, plan P3.2) | rusttype's Scale already normalises to the ascent+|descent| span — the SWF path's em model at factor 1.0; the TTF path is dead on live scenes (font telemetry: 110/110 resolved-record-symbol on both Clipper scenes — the shared fontlib merges into every binding); all five target renders byte-identical except the reference-adjudicated caption pairs; 517 lib tests green. |
| `ir_compose` | `SWF_TEXT_RENDER_SIZE_CALIBRATION = 0.84` + `INLINE_NESTED_TEXTFIELD_WORD_GAP = 0.33` (inline nested-pair width + word gap) | Retired (2026-06-13, plan P3.3/P3.4) | Replaced by advance-at-draw-size: the child continues the parent's inline run at the parent's advance end; bearings supply the visible gap (medical1 capture: "3"->"M" ink gap ~3px, letter-gap scale). T3->M offset 54 vs capture 55, unchanged. Slug rasteriser CONFIRMED from binary strings (Terathon::Slug::FontHeader, CSlugPipeline) — runbook updated. |
| `ir_compose` | `LABEL_CAPTION_PAIR_FLEX_ROW_SPACING = -8.0` + `line_box_overlap` subtraction (caption-pair handoff) | Retired (2026-06-13, plan P3.4) | Pure line-box stack restored (the ORIGINAL derivation, commit 020191279): value line top = label line top + label em; reproduced only by -8/overlap against the retired inflated heights (45-7-8 ≈ 30). ui_measure.py on medical1: MEDGELS top-to-top capture 29, render 28 -> 29 (exact); PATIENT NAME 1010 -> 1004 (capture 1004, exact — its known-outlier entries graduated). |
| `crates/starbreaker-ui/src/bb_layout/engine_01.rs` | `ADDITIVE_ALTERNATE_REVERSE_POSITION_PHASE_RATIO = 2/3` static-sampling phase | Retired (2026-06-12) | Empirical audit: no frozen pin referenced it; additive AlternateReverse loops sample at the requested midpoint (full battery green). |
| `crates/starbreaker-ui/src/ui_ir/engine_02.rs` | `textfield_fallback_font_size_from_signals` hand-tuned per-style rect ladder (Title4→56, Title3→90, Heading2/3/6 rows, `LONG_HEADING1_PROMPT_FONT_SIZE = 28.7`) | Retired (2026-06-12) | Empirical audit: no frozen pin references any rung (full battery + font harness green with the ladder disabled); the brand typography table covers the styled cases. Deleted outright. |
| `crates/starbreaker-ui/src/ui_ir/engine_02.rs` | `fixed_band_heading_prompt_size` / `FIXED_BAND_HEADING_FILL = 0.381` calibrated banner-prompt reduction | Retired (2026-06-12, with the text-format route) | The 0.381×105 ≈ 40.0px output was reverse-engineering the AUTHORED mainmenu bioc entry `FontSize=40` (Parent(UI_Generic_Flag_01)), now applied via the textfield text-format route. Deleted; `ui_check.sh --full` ALL GREEN incl. font harness 26/26 — zero drift. |
| `crates/starbreaker-ui/src/style/loader.rs` | "Drake amber" fallback palette (invented colours present in no DataCore record), also leaked into per-slot defaults when parsing real records | Retired (commit 5bf1d7f84, 2026-06-12) | Replaced by `neutral_fallback` (white-on-black, no palette); the `rgba_colour_literals_are_not_hardcoded` guard bans reintroduction. |
| `crates/starbreaker-ui/src/ui_ir/...` | `CAPS_FORCED_UPPER_FONT_SCALE = 0.98` all-caps reduction | Retired (2026-06; see project memory "caps reduction removed" and `crates/starbreaker-ui/docs/ui-workflow.md` §10) | The width rationale was false (the reference RTT is ~2% small); styled text renders the full brand nominal; all four standards re-frozen. |
| `crates/starbreaker-ui/src/pipeline.rs` | Pipeline-local defaults builder and split fallback policy | Retired | Consolidated to `DefaultValueRegistry::with_pipeline_defaults(...)` in Phase 4 |
| `crates/starbreaker-ui/src/ir_compose.rs` | Name/path-based screen/manufacturer hardcoded rendering branches | Retired | Eliminated in Phase 2B source-backed IR pass and guarded by `.github/scripts/check_ui_hardcoding.sh` |
| `crates/starbreaker-ui/src/pipeline/swf_selection/flash_paths.rs` | Hard-coded `brand_ship_subdirs` / `annunciator_ship_subdirs` ship lists for SWF path probing | Retired (Phase 1, 2026-06-05) | Replaced by `p4k_ship_subdirs` which enumerates P4K brand dir dynamically; no hard-coded ship names remain. |

## Operational Policy

- Every fallback in production must be listed here.
- New fallbacks must declare scope, trigger, and sunset target before merge.
- Fall-backs without telemetry or retirement criteria are not allowed.
- When a fallback is removed, move it to the retired section with evidence.

## Pinned at-rest registry values

Pinned (non-derived) values in
`crates/starbreaker-ui/data/default_value_registry_v1.json` are themselves
documented fallbacks: each path family's provenance and sunset condition is
tracked in `crates/starbreaker-ui/data/default_value_registry_v1.notes.md`
(updated in the same commits that touch the registry).
